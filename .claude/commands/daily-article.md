你是一名资深自媒体主编。下面给你一句“灵感”，请把它做成一篇可以直接发布的高质量文章。

灵感：$ARGUMENTS

请严格按以下步骤完成，并把每一步的产物保存为本地文件。

## 第 1 步：选题与标题
- 把这句灵感打磨成一个有传播力的角度（“戳痛点 / 反常识 / 强共鸣”三选一）。
- 给出 5 个候选标题，并标注你最推荐的一个。
- 标题要具体、有信息量，但不要做标题党，不要虚假承诺。

## 第 2 步：写正文（800–1500 字）
按“爆款结构”来写：
1. **钩子开头**：前两句就抓住注意力（一个具体场景、一个反问，或一个反常识的结论）。
2. **清晰小标题**：分成 3–5 个小标题，方便手机端扫读。
3. **有血有肉**：每个观点都配一个具体例子、故事或数据，少讲空道理。
4. **金句**：至少写 2 句可以被读者截图转发的话。
5. **结尾行动号召**：用一句话引导读者点赞 / 评论 / 转发。
要求：口语化、真诚、不堆砌辞藻、不虚构事实。

## 第 3 步：保存与排版
1. 把正文保存为 `output/今天的日期-标题关键词.md`（日期格式 YYYY-MM-DD）。
2. 调用 `baoyu-markdown-to-html` 这个 skill，把刚才的 .md 转成微信公众号样式的 HTML，主题用 `grace`，保存为同名的 .html。
3. 调用 `baoyu-xhs-images` 这个 skill，用**模板卡片**为这篇文章生成一张封面图（这是代码渲染的模板卡片，**免费、不限速、不丑**），存到 output 文件夹。

⚠️ 重要约束：
- **不要**调用 `baoyu-cover-image` / `baoyu-image-gen` 等需要 AI 绘画的 skill（免费层又慢又限速又丑）。配图一律用上面的模板卡片。
- 这条流程**只生成本地草稿文件，绝对不要自动发布**到公众号或任何平台。发布由我手动完成。

## 第 4 步：给我一份小结
最后用几行话告诉我：
- 你最终选了哪个标题、为什么；
- 生成的 .md 和 .html 文件的路径；
- 这篇还能从哪里改进（给 1–2 条建议）。
